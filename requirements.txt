# smarthr360-policy-gen — AI HR policy generator (Module 4)
Django==5.2.8
djangorestframework>=3.15,<4.0
drf-spectacular>=0.27,<1.0
django-cors-headers>=4.3,<5.0
python-decouple>=3.8,<4.0
dj-database-url>=2.1,<3.0
gunicorn>=21.2,<22.0
whitenoise>=6.6,<7.0
psycopg2-binary>=2.9,<3.0

# Shared platform JWT client (RS256 local verification).
# Shared platform JWT client — vendored wheel (built from smarthr360-platform/packages/smarthr360-jwt-auth).
./smarthr360_jwt_auth-1.1.1-py3-none-any.whl

# Shared enterprise-integration toolkit (observability, API envelope) —
# vendored wheel built from smarthr360-platform/packages/smarthr360-integration.
./smarthr360_integration-0.1.0-py3-none-any.whl

# LLM recommendations (optional: static fallback without it)
groq>=0.9
python-dotenv>=1.0

# Sibling service calls
requests>=2.31

# Observability
django-prometheus>=2.3
